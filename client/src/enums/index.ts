export enum Auth_Method {
  EMAIL = "email",
  GOOGLE = "google",
}

export enum IssueTypes {
  OPEN = "Open",
  INPROGRESS = "In Progress",
  RESOLVED = "Resolved",
  CLOSED = "Closed",
}

export enum ResponseStatus {
  FULFILLED = "fulfilled",
  REJECTED = "rejected",
}
